const express = require("express");
const Login = require("../Database");
// const Mapping = require("./Operations");
const Router = express.Router();

Router.post("/Insert", (req, res) => {
  let Details = req.body;
  console.log(Details);
  Login.Login("Login", "Insert", Details)
    .then((result) => {
      res.send(result);
      console.log(result);
    })
    .catch((err) => {
      res.send(err);
    });
});
Router.delete("/Delete:username", (req, res) => {
  let Details = req.params.username;
  Login.Login("Login", "Delete", Details)
    .then((result) => {
      res.send(result);
      console.log(result);
    })
    .catch((err) => {
      res.send(err);
    });
});
Router.put("/Update:username", (req, res) => {
  let Details = req.params.username;
  let UpdatedDetails = req.body;
  console.log(UpdatedDetails);
  console.log(Details);
 Login.Login("Login", "Update", Details, UpdatedDetails)
    .then((result) => {
      res.send(result);
      console.log(result);
    })
    .catch((err) => {
      res.send(err);
    });
});
Router.get("/Read:username", (req, res) => {
  const Details = req.params.username;
  console.log(Details);
  Login.Login("Login", "Read", Details)
    .then((result) => {
      res.send({ Message: result.Message, Result: result.rows });
      console.log(result);
    })
    .catch((err) => {
      res.send(err);
    });
});

module.exports = Router;